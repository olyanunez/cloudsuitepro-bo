import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

// module.exports = {
//   async rewrites() {
//     return [
//       {
//         source: '/:path*',  // Cualquier ruta solicitada
//         destination: '/pages/:path*',  // Reescribe la URL agregando /pages
//       },
//     ];
//   },
// };

export default nextConfig;
